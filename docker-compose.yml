version: "3.8"
services:
  nfce-scanner:
    image: nfce-scanner:latest
    ports:
      - '8000:8000'
    shm_size: 2gb
    environment:
      - API_KEY=${API_KEY}
      # Uncomment and set PROXY_URL if the server IP triggers captcha:
      # - PROXY_URL=http://user:pass@proxy:port